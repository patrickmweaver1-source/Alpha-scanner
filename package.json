{
  "name": "alpha-scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Crypto perpetuals + US options alpha scanner with AI verdict",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {}
}
